'use client';

import { CustomersTableType } from "@/app/lib/definitions";

export default function Form({ customer }: { customer: CustomersTableType }) {
    return (
        <div>Create Customer</div>
    );
}
